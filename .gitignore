```gitignore
# Excel files
*.xlsx

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
.ENV
.env
*.env
.env.*
.env.local
.env.dev
.env.development
.env.prod
.env.production
.env.test
.env.testing
.env.staging
.env.preview
.env.backup
.coverage
coverage/
htmlcov/
.pytest_cache/
.mypy_cache/
.hypothesis/
*.log
*.sqlite3
*.sqlite3-journal
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*.tmp
```

Wait, I need to reconsider. The only files mentioned are `data/summary.xlsx` and `scripts/extract.py`. An Excel file should not be ignored as it appears to be data for the project, and `.py` files are source code that shouldn't be ignored. Let me re-evaluate:

Since the changes include only source/config files (`.xlsx` and `.py`) and no build artifacts, dependencies, or temp files, the output should be empty.

```gitignore

```